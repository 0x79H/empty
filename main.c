#pragma comment(linker, "/subsystem:windows /ENTRY:mainCRTStartup") 
#include <stdio.h>
#include <getopt.h>

int main(int argc,char *argv[]){
    FILE *fp;
    filename = 'D:\empty_out.log'
    //if argv[1]=out -->filename=argv[2]
	made = a+;
    if(fp = fopen(char* filename, char* made)) exit(0);
    for (int i=1,i<=argc,i++){
        fprintf(fp,"%s",argv[i]);
        }
    fclose(fp);
    return 0;
    }
